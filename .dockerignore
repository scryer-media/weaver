.git
.github
.vscode
**/worktrees
**/.worktrees

target
target-*
**/target
fuzz/artifacts
tmp
*.db
*.db-*

apps/weaver-web/node_modules
apps/weaver-web/dist

# The release image is built from this context but never consumes the E2E
# harness. Keeping it out lets harness-only changes reuse the existing image.
e2e

crates/*/tests
crates/*/examples

docs
# The upgrade holding page is served before the web bundle exists, so the
# server embeds the lockup straight from here.
!docs/img/weaver-lockup-on-dark.svg
scripts
tasks
tests
